# Local dev: Vite proxies /api → http://127.0.0.1:8001 (see vite.config.js)
# Start backend first: cd ../backend && npm run dev
VITE_API_URL=/api

# Or call backend directly (no Vite proxy):
# VITE_API_URL=http://localhost:8001/api

# Production:
# VITE_API_URL=https://your-api.example.com/api
