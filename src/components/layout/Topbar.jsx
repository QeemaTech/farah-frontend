/**
 * Top bar UI is composed inside `admin/components/AdminLayout.jsx`
 * (title, search, language, theme, notifications, user menu).
 * This file exists for the requested layout folder structure.
 */
export default function TopbarPlaceholder() {
  return null
}
